// Placeholder — the blog editor is built in Phase B.
export default function BlogTab() {
  return (
    <div className="empty-state card">
      <p>The blog editor is coming next.</p>
    </div>
  );
}
